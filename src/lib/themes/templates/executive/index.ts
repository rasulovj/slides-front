// lib/themes/templates/executive/index.ts
import { Theme } from "../../types";
import { executiveConfig } from "./config";
import { ExecutiveLayouts } from "./layout";

export const ExecutiveTheme: Theme = {
  config: executiveConfig,
  layouts: ExecutiveLayouts,
};
